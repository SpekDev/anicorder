[project]
name = "anicorder"
version = "0.0.1"
description = "A terminal appliction for managing shows and anime's watched"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mariadb>=1.1.14",
    "textual>=8.2.8",
    "titlecase>=2.4.1",
]
